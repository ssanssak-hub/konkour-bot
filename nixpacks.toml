[phases.setup]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build completed successfully!'"]

[start]
cmd = "python main.py"

[environment]
PYTHON_VERSION = "3.11"
